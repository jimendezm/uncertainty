[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "--cov=utils --cov=probability --cov-report=term-missing"
testpaths = ["tests"]

[tool.coverage.run]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 80
